package org.example.account.dto;

import java.math.BigDecimal;

public record BankAccountResponse(
        Long id,
        Long userId,
        String accountType,
        String currency,
        BigDecimal balance
) {}
