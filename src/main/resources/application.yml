spring:
  security:
    user:
      name: admin
      password: admin123

  springdoc:
    swagger-ui:
      path: /swagger-ui.html

  datasource:
    url: jdbc:h2:mem:eaglebank;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa00
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.format_sql: true

  h2:
    console:
      enabled: true
      path: /h2-console

server:
  port: 8080

app:
  jwt:
    secret: "change-me-super-long-random-hex"   # 256-bit+ secret
    expiry-minutes: 60

  datasource:
    url: jdbc:postgresql://localhost:5432/eaglebank
    username: eagle
    password: eagle
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate.jdbc.time_zone: UTC
      hibernate.format_sql: true
      hibernate.default_schema: public
  flyway:
    enabled: true
